import React from 'react';
import { Route } from '../react-router-dom';
import './navLink.css';
export default function NavLink(props) {
  const {to, exact, component} = props;
  return(
    <Route
    
    >

    </Route>
  )
}